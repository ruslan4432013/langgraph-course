# LangChain Embeddings Examples

Репозиторий с примерами работы с эмбеддингами в LangChain — от базового API до практического семантического поиска.

## 📋 Содержание

- [Описание](#описание)
- [Требования](#требования)
- [Установка](#установка)
- [Структура проекта](#структура-проекта)
- [Примеры](#примеры)
- [Настройка](#настройка)

## Описание

Эмбеддинги (векторные представления) позволяют преобразовывать текст в числовые векторы, сохраняющие семантический
смысл. Это основа для:

- 🔍 Семантического поиска
- 📊 Кластеризации документов
- 🤖 RAG-систем (Retrieval-Augmented Generation)
- 📈 Рекомендательных систем

## Требования

- Python 3.10+
- OpenAI API ключ (или совместимый API)

## Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd langchain-embeddings-examples

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install langchain-openai
```

## Структура проекта

```
.
├── src/
│   └── settings.py          # Конфигурация (API ключи)
├── 01_basic_interface.py    # Базовый интерфейс эмбеддингов
├── 02_similarity_metrics.py # Метрики сходства векторов
├── 03_semantic_search.py    # Практический семантический поиск
└── README.md
```

## Примеры

### 1. Базовый интерфейс (`01_basic_interface.py`)

Знакомство с основными методами LangChain для работы с эмбеддингами:

| Метод               | Назначение                                          |
|---------------------|-----------------------------------------------------|
| `embed_documents()` | Векторизация списка документов (батчевая обработка) |
| `embed_query()`     | Векторизация одного поискового запроса              |

```bash
python 01_basic_interface.py
```

### 2. Метрики сходства (`02_similarity_metrics.py`)

Реализация трёх основных метрик для сравнения векторов:

| Метрика                    | Диапазон | Интерпретация                   |
|----------------------------|----------|---------------------------------|
| **Косинусное сходство**    | [-1, 1]  | 1 = идентичны, 0 = ортогональны |
| **Евклидово расстояние**   | [0, ∞)   | Чем меньше, тем ближе           |
| **Скалярное произведение** | (-∞, +∞) | Зависит от длины векторов       |

```bash
python 02_similarity_metrics.py
```

### 3. Семантический поиск (`03_semantic_search.py`)

Практический пример: поиск документов **по смыслу**, а не по ключевым словам.

```
Запрос: "Какие бывают домашние любимцы?"

Результаты:
[0.8734] Кошки - это домашние животные семейства кошачьих
[0.8521] Хомяки - популярные домашние питомцы
[0.8103] Собаки считаются лучшими друзьями человека
[0.7012] Python используется для анализа данных
...
```

> 💡 Обратите внимание: запрос не содержит слов "кошка", "собака" или "хомяк", но семантический поиск корректно находит
> релевантные документы!

```bash
python 03_semantic_search.py
```

## Настройка

Создайте файл `src/settings.py`:

```python
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    OPENAI_API_KEY: str
    OPENAI_BASE_URL: str = "https://api.openai.com/v1"

    class Config:
        env_file = ".env"


settings = Settings()
```

Создайте `.env` файл в корне проекта:

```env
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1  # опционально
```

## 📚 Полезные ссылки

- [LangChain Embeddings Documentation](https://python.langchain.com/docs/concepts/embedding_models/)
- [OpenAI Embeddings Guide](https://platform.openai.com/docs/guides/embeddings)
