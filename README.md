# Курс по LangGraph: Retrieval (Поиск) в LLM

Этот репозиторий содержит обучающие материалы и примеры кода, посвященные концепциям Retrieval (RAG) и созданию
интеллектуальных поисковых агентов с использованием LangGraph.

## 📁 Структура проекта

### 💻 Примеры реализации (src/retrieval)

* **`agentic_rag_simple.py`** — Базовый пример Agentic RAG. Агент использует инструмент `fetch_url` для получения данных
  по запросу.
* **`agentic_rag_langgraph.py`** — Продвинутый пример, использующий возможности LangGraph для создания циклической
  логики и работы с документацией Anthropic.

## 🚀 Архитектуры, рассматриваемые в курсе

1. **2-Step RAG (Классический)**: Линейный процесс — сначала поиск в базе знаний, затем генерация ответа на основе
   найденного контекста.
2. **Agentic RAG**: Агент сам принимает решение, нужно ли ему искать информацию и какие инструменты для этого
   использовать.
3. **Hybrid RAG / Self-Correction**: Системы с обратной связью, которые проверяют релевантность найденных документов и
   могут корректировать поиск или ответ.

## 🛠 Инструкция по установке

1. **Клонируйте репозиторий**
2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```
3. **Настройте окружение**:
   Создайте файл `.env` (на базе `src/settings.py`) и добавьте необходимые API ключи (например, `OPENAI_API_KEY` для
   ProxyAPI или Anthropic).

## 🏃 Запуск примеров

Примеры запускаются из корня проекта:

```bash
python src/retrieval/agentic_rag_simple.py
python src/retrieval/agentic_rag_langgraph.py
```

---
*Проект подготовлен для обучения современным методам работы с LLM и LangGraph.*
