"""
LangGraph: Строим простой граф

Этот модуль демонстрирует создание простого графа с помощью библиотеки LangGraph.
Граф состоит из трёх узлов и условного ребра, которое случайным образом выбирает
между положительной и отрицательной эмоцией.

Структура графа:
START → node_1 → (node_2 или node_3) → END

Автор: Демонстрация для видео урока
"""

import random
from typing import TypedDict, Literal

from langgraph.constants import START, END
from langgraph.graph import StateGraph


# ==================== СОСТОЯНИЕ ГРАФА ====================

class State(TypedDict):
    """
    Состояние графа - это словарь, который передаётся между узлами.

    В LangGraph состояние служит памятью для обмена данными между этапами графа.
    Каждый узел может читать и изменять это состояние.

    Attributes:
        graph_state (str): Строка, которая накапливает текст по мере прохождения через узлы
    """
    graph_state: str


# ==================== УЗЛЫ ГРАФА ====================

def node_1(state: State) -> State:
    """
    Первый узел графа - добавляет фразу "I am" к состоянию.

    Узлы в LangGraph - это обычные Python-функции, которые:
    1. Принимают текущее состояние
    2. Выполняют некоторую логику
    3. Возвращают обновлённое состояние

    Args:
        state (State): Текущее состояние графа

    Returns:
        State: Обновлённое состояние с добавленной фразой "I am"
    """
    print('---Node 1---')  # Отладочный вывод для отслеживания выполнения
    return {'graph_state': state['graph_state'] + ' I am'}


def node_2(state: State) -> State:
    """
    Второй узел графа - добавляет положительную эмоцию "happy!".

    Этот узел выполняется, если условное ребро выберет позитивный путь.

    Args:
        state (State): Текущее состояние графа

    Returns:
        State: Состояние с добавленной фразой " happy!"
    """
    print('---Node 2---')  # Отладочный вывод
    return {'graph_state': state['graph_state'] + ' happy!'}


def node_3(state: State) -> State:
    """
    Третий узел графа - добавляет отрицательную эмоцию "sad!".

    Этот узел выполняется, если условное ребро выберет негативный путь.

    Args:
        state (State): Текущее состояние графа

    Returns:
        State: Состояние с добавленной фразой " sad!"
    """
    print('---Node 3---')  # Отладочный вывод
    return {'graph_state': state['graph_state'] + ' sad!'}


# ==================== УСЛОВНЫЕ РЁБРА ====================

def decide_mood(state: State) -> Literal['node_2', 'node_3']:
    """
    Функция условного ребра - решает, к какому узлу перейти дальше.

    Рёбра связывают узлы в графе. Условные рёбра позволяют выбирать
    следующий узел на основе логики или случайности.

    В реальных задачах здесь может быть:
    - Анализ текста (sentiment analysis)
    - Проверка условий на основе данных пользователя
    - Логика на основе предыдущих результатов

    В нашем примере используется простая случайность с шансом 50/50.

    Args:
        state (State): Текущее состояние графа

    Returns:
        Literal['node_2', 'node_3']: Имя следующего узла для выполнения
    """
    print('---Decide Mood---')  # Отладочный вывод

    # Получаем текущее состояние (в реальных задачах можно анализировать содержимое)
    user_input = state['graph_state']

    # Случайный выбор между узлами с вероятностью 50/50
    if random.random() < 0.5:
        print('Выбран позитивный путь -> node_2')
        return 'node_2'
    else:
        print('Выбран негативный путь -> node_3')
        return 'node_3'


# ==================== ПОСТРОЕНИЕ ГРАФА ====================

# Создаём построитель графа с определённым типом состояния
builder = StateGraph(State)

# Добавляем узлы в граф
# Каждый узел связывается с соответствующей функцией
builder.add_node('node_1', node_1)
builder.add_node('node_2', node_2)
builder.add_node('node_3', node_3)

# Добавляем рёбра (связи между узлами)
builder.add_edge(START, 'node_1')  # Стартовая точка -> node_1

# Условное ребро: от node_1 переход определяется функцией decide_mood
builder.add_conditional_edges('node_1', decide_mood)

# Обычные рёбра: node_2 и node_3 завершаются в конечной точке
builder.add_edge('node_2', END)
builder.add_edge('node_3', END)

# Компилируем граф для выполнения
graph = builder.compile()

# ==================== ЗАПУСК ГРАФА ====================

if __name__ == '__main__':
    """
    Демонстрация работы графа.

    Граф выполняется синхронно методом invoke():
    1. Начинается со START
    2. Переходит к node_1, который добавляет "I am"
    3. decide_mood случайно выбирает node_2 или node_3
    4. Выбранный узел добавляет эмоцию ("happy!" или "sad!")
    5. Завершается в END
    """

    print("🚀 Запускаем граф LangGraph!")
    print("=" * 40)

    # Начальное состояние с приветствием
    initial_state = {'graph_state': 'Hello! My name is Ruslan.'}
    print(f"Начальное состояние: {initial_state['graph_state']}")
    print()

    # Выполняем граф
    result = graph.invoke(initial_state)

    print()
    print("=" * 40)
    print(f"🎉 Итоговый результат: {result['graph_state']}")
    print()
    print("Граф успешно выполнен! Попробуйте запустить ещё раз - результат может отличаться.")